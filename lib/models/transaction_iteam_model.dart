class TransactionIteamModel {
  final String title, date, amount;
  final bool isWithDrawel;

  const TransactionIteamModel(
      {required this.title,
      required this.date,
      required this.amount,
      required this.isWithDrawel});
}
